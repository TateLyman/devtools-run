import type { Metadata } from "next";
export const metadata: Metadata = { title: "SVG Wave Generator - Create Section Dividers Free", description: "Generate SVG wave dividers for your website sections. Adjust amplitude, frequency, color. Copy SVG or CSS. Free wave generator.", keywords: ["svg wave generator", "wave divider css", "section divider svg", "wave background", "svg wave maker"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
