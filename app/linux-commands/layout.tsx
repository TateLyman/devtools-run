import type { Metadata } from "next";
export const metadata: Metadata = { title: "Linux Commands - Essential Linux terminal commands Free", description: "Essential Linux terminal commands. Free Linux Commands.", keywords: ["linux terminal", "linux-commands", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
