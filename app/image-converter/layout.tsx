import { Metadata } from "next";
export const metadata: Metadata = {
  title: "Convert Image Format Online — PNG, JPG, WebP, BMP",
  description: "Convert images between PNG, JPEG, WebP, and BMP formats. Adjust quality settings. Free, fast, runs in your browser.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
