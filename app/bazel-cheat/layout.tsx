import type { Metadata } from "next";
export const metadata: Metadata = { title: "Bazel Cheatsheet - Bazel build reference Free", description: "Bazel build reference. Free Bazel Cheatsheet.", keywords: ["bazel reference", "bazel-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
