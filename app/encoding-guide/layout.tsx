import type { Metadata } from "next";
export const metadata: Metadata = { title: "Character Encoding Guide - Character encoding reference Free", description: "Character encoding reference. Free Character Encoding Guide.", keywords: ["utf8 ascii", "encoding-guide"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
