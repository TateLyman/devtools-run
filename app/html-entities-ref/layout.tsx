import type { Metadata } from "next";
export const metadata: Metadata = { title: "HTML Entities Table - Complete HTML entities table Free", description: "Complete HTML entities table. Searchable reference. Free HTML Entities Table.", keywords: ["html special characters", "html-entities-ref", "reference", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
