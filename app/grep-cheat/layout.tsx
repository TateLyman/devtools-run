import type { Metadata } from "next";
export const metadata: Metadata = { title: "Grep Cheatsheet - Grep search reference Free", description: "Grep search reference. Free Grep Cheatsheet.", keywords: ["grep commands", "grep-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
