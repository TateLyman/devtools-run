import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "URL Encoder / Decoder - Encode URLs Online Free",
  description: "Encode and decode URLs and query parameters online. Supports encodeURIComponent and encodeURI. Free URL encoder and decoder tool.",
  keywords: ["URL encoder", "URL decoder", "encode URL online", "decode URL", "percent encoding", "URL encoding tool"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
