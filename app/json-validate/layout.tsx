import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "JSON Validator - Validate JSON Syntax Online Free",
  description: "Validate JSON syntax instantly. See detailed error messages with line numbers. Check if JSON is valid. Free JSON validator tool.",
  keywords: ["json validator", "validate json", "json syntax check", "is json valid", "json checker", "json lint"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
