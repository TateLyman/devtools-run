import type { Metadata } from "next";
export const metadata: Metadata = { title: "SMTP Guide - SMTP email protocol reference Free", description: "SMTP email protocol reference. Free SMTP Guide.", keywords: ["smtp protocol", "smtp-guide", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
