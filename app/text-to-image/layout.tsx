import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Text to Image Generator — Create Quote Images Free",
  description: "Create beautiful quote images for social media. Gradient backgrounds, custom fonts, author attribution, size presets for Instagram, Twitter, LinkedIn. Download as PNG.",
  keywords: ["text to image", "quote maker", "quote image generator", "social media quote", "instagram quote", "text to PNG", "quote card maker"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
