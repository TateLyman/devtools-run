import type { Metadata } from "next";
export const metadata: Metadata = { title: "DNS Guide - DNS record types explained Free", description: "DNS record types explained. Free DNS Guide.", keywords: ["dns records", "dns-guide", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
