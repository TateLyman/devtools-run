import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Screenshot to HTML/CSS Code — Free Online Converter",
  description: "Upload a screenshot, trace sections on it, and generate clean HTML/CSS layout code. Canvas-based editor. Free screenshot to code converter tool.",
  keywords: ["screenshot to code", "image to HTML", "screenshot to HTML", "image to CSS", "UI to code", "design to code", "screenshot converter"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
