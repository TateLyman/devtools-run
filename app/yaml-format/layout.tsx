import type { Metadata } from "next";
export const metadata: Metadata = { title: "YAML Formatter - Format and validate YAML Free Online", description: "Format and validate YAML. Paste content, get formatted output. Free YAML Formatter.", keywords: ["format yaml", "yaml-format", "formatter", "online tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
