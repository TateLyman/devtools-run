import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "JSON String Escape/Unescape - Escape JSON Strings Free",
  description: "Escape and unescape JSON strings. Handle special characters, newlines, quotes. Free JSON string escape tool.",
  keywords: ["json escape", "json unescape", "escape json string", "json string encode", "json special characters"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
