import type { Metadata } from "next";
export const metadata: Metadata = { title: "Wget Cheatsheet - Wget download reference Free", description: "Wget download reference. Free Wget Cheatsheet.", keywords: ["wget commands", "wget-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
