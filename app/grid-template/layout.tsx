import type { Metadata } from "next";
export const metadata: Metadata = { title: "CSS Grid Template Areas - Visual Grid Area Builder Free", description: "Build CSS grid-template-areas visually. Name grid areas, drag to resize. Copy CSS. Free CSS grid template builder.", keywords: ["grid template areas", "CSS grid areas", "grid layout builder", "named grid areas", "CSS grid template"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
