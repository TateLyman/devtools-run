import type { Metadata } from "next";
export const metadata: Metadata = { title: "Netlify CLI Reference - Netlify CLI commands Free", description: "Netlify CLI commands. Searchable. Free Netlify CLI Reference.", keywords: ["netlify commands", "netlify-cheat", "cheatsheet", "cloud reference"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
