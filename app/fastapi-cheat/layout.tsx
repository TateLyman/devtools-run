import type { Metadata } from "next";
export const metadata: Metadata = { title: "FastAPI Cheatsheet - FastAPI Python reference Free", description: "FastAPI Python reference. Free FastAPI Cheatsheet.", keywords: ["fastapi reference", "fastapi-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
