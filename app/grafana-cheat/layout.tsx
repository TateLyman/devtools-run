import type { Metadata } from "next";
export const metadata: Metadata = { title: "Grafana Cheatsheet - Grafana dashboard reference Free", description: "Grafana dashboard reference. Free Grafana Cheatsheet.", keywords: ["grafana reference", "grafana-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
