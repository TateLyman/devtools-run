import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Sitemap.xml Generator - Create XML Sitemaps Online Free",
  description: "Generate valid sitemap.xml files for your website. Enter URLs, set change frequency and priority. Download or copy. Free online sitemap generator.",
  keywords: ["sitemap generator", "XML sitemap", "sitemap.xml maker", "create sitemap", "SEO sitemap", "website sitemap generator"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
