import type { Metadata } from "next";
export const metadata: Metadata = { title: "gRPC Guide - gRPC protocol reference Free", description: "gRPC protocol reference. Comprehensive reference. Free gRPC Guide.", keywords: ["grpc tutorial", "grpc-guide", "guide", "tutorial"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
