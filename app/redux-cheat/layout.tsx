import type { Metadata } from "next";
export const metadata: Metadata = { title: "Redux Cheatsheet - Redux state management reference Free", description: "Redux state management reference. Free Redux Cheatsheet.", keywords: ["redux reference", "redux-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
