import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "URL Encoder/Decoder - Encode & Decode URLs Free Online",
  description: "Encode and decode URLs instantly. URL encode special characters or decode percent-encoded strings. Free URL encoder decoder tool.",
  keywords: ["url encoder", "url decoder", "url encode", "url decode", "percent encoding", "url escape"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
