import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "PX to REM Converter - Free Pixel to REM/EM Calculator",
  description: "Convert pixels to rem, em, pt, vw, vh instantly. Set base font size. Conversion table. Free px to rem converter for CSS developers.",
  keywords: ["px to rem", "pixel to rem", "rem converter", "px to em", "CSS unit converter", "px rem calculator"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
