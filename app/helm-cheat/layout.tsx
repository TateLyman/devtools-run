import type { Metadata } from "next";
export const metadata: Metadata = { title: "Helm Cheatsheet - Kubernetes Helm charts reference Free", description: "Kubernetes Helm charts reference. Free Helm Cheatsheet.", keywords: ["helm charts", "helm-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
