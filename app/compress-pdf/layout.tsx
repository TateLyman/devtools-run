import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Compress PDF Online — Reduce PDF File Size Free",
  description:
    "Reduce PDF file size in your browser. See before/after comparison, adjust quality. No upload, no signup, completely free PDF compressor.",
  keywords: [
    "compress PDF",
    "reduce PDF size",
    "PDF compressor",
    "shrink PDF",
    "optimize PDF",
    "free PDF compression",
  ],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
