import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "SVG to CSS Background - Convert SVG to Data URI Online",
  description: "Convert SVG code to CSS background-image data URI instantly. URI encoded or Base64. No external files needed. Free online tool for web developers.",
  keywords: ["SVG to CSS", "SVG data URI", "SVG background image", "SVG to base64", "CSS background SVG", "inline SVG CSS"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
