import type { Metadata } from "next";
export const metadata: Metadata = { title: "ASCII Reference Table - Complete ASCII code table Free", description: "Complete ASCII code table. Free ASCII Reference Table.", keywords: ["ascii codes table", "ascii-reference"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
