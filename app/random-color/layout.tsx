import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Random Color Generator - Generate Random HEX Colors Free",
  description: "Generate random colors with hex code, RGB, and HSL values. Lock favorite colors. Build palettes. Free random color generator.",
  keywords: ["random color generator", "random hex color", "color randomizer", "generate random color", "random color picker"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
