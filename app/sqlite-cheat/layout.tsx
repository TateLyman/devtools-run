import type { Metadata } from "next";
export const metadata: Metadata = { title: "SQLite Cheatsheet - SQLite query reference Free", description: "SQLite query reference. Free SQLite Cheatsheet.", keywords: ["sqlite commands", "sqlite-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
