import type { Metadata } from "next";
export const metadata: Metadata = { title: "Arrow Guide - Apache Arrow format guide Free", description: "Apache Arrow format guide. Free Arrow Guide.", keywords: ["apache arrow", "arrow-guide", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
