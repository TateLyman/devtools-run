import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Border Radius Generator - Visual Corner Radius Tool Free",
  description: "Generate CSS border-radius visually. Individual corner control. Live preview. Copy CSS code. Free border radius generator tool.",
  keywords: ["border radius generator", "CSS border radius", "rounded corners", "border radius tool", "CSS corner radius"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
