import type { Metadata } from "next";
export const metadata: Metadata = { title: "Glassmorphism Card Generator - Frosted Glass CSS Free", description: "Create glassmorphism cards with CSS. Adjust blur, opacity, border radius, color. Live preview. Copy CSS. Free glassmorphism generator.", keywords: ["glassmorphism", "frosted glass css", "glass effect css", "glassmorphism generator", "glass card css"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
