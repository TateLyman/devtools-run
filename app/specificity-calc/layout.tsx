import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Specificity Calculator - Calculate Selector Specificity Free",
  description: "Calculate CSS selector specificity. Enter any CSS selector, see its specificity score. Compare selectors. Free specificity calculator.",
  keywords: ["CSS specificity", "specificity calculator", "CSS selector specificity", "specificity score", "CSS priority", "selector weight"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
