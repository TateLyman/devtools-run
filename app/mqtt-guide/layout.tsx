import type { Metadata } from "next";
export const metadata: Metadata = { title: "MQTT Guide - MQTT IoT protocol reference Free", description: "MQTT IoT protocol reference. Free MQTT Guide.", keywords: ["mqtt protocol", "mqtt-guide", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
