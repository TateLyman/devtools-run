import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Regex Tester - Test Regular Expressions Online Free",
  description: "Test regex patterns with real-time match highlighting. Common presets for email, URL, IP, phone. Flags support. Free online regex tester and debugger.",
  keywords: ["regex tester", "regex101 alternative", "regular expression tester", "regex debugger", "test regex online", "regex matcher"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
