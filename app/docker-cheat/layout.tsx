import type { Metadata } from "next";
export const metadata: Metadata = { title: "Docker Cheatsheet - Quick Reference Free", description: "Essential Docker commands reference. Searchable. Click to copy. Free Docker Cheatsheet.", keywords: ["docker commands cheatsheet", "docker-cheat", "reference", "commands"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
