import type { Metadata } from "next";
export const metadata: Metadata = { title: "Solana CLI Cheatsheet - Solana CLI command reference Free", description: "Solana CLI command reference. Free Solana CLI Cheatsheet.", keywords: ["solana cli", "solana-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
