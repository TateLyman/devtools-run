import type { Metadata } from "next";
export const metadata: Metadata = { title: "Viewport Size - Live Browser Window Dimensions Free", description: "See your browser viewport dimensions in real-time. Resize to see changes. Width, height, aspect ratio. Free viewport size tool.", keywords: ["viewport size", "browser size", "window dimensions", "viewport width height", "responsive test"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
