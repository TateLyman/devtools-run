import type { Metadata } from "next";
export const metadata: Metadata = { title: "Deno Cheatsheet - Deno runtime reference Free", description: "Deno runtime reference. Free Deno Cheatsheet.", keywords: ["deno runtime", "deno-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
