import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Image to Base64 Converter - Encode Images Online Free",
  description: "Convert images to Base64 encoded strings. Get raw Base64, CSS background, HTML img tag, and data URI. Drag & drop upload. Free, no server upload.",
  keywords: ["image to base64", "base64 encoder", "image encoder", "base64 converter", "data URI generator", "inline image"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
