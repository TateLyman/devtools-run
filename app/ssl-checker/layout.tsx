import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "SSL Certificate Checker - Check SSL/TLS Grade Online Free",
  description: "Check SSL/TLS certificate status and grade for any domain. Powered by SSL Labs. See certificate details and security rating. Free SSL checker.",
  keywords: ["SSL checker", "SSL certificate check", "TLS checker", "SSL grade", "HTTPS checker", "SSL test online"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
