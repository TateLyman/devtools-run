import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Regex Cheatsheet - Regular Expression Quick Reference Free",
  description: "Complete regex cheatsheet with examples. Character classes, quantifiers, anchors, groups, lookahead, lookbehind. Free regex reference.",
  keywords: ["regex cheatsheet", "regular expression cheatsheet", "regex reference", "regex guide", "regex syntax", "regex patterns"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
