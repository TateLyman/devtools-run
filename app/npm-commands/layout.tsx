import type { Metadata } from "next";
export const metadata: Metadata = { title: "NPM Commands - NPM package manager commands Free", description: "NPM package manager commands. Free NPM Commands.", keywords: ["npm reference", "npm-commands", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
