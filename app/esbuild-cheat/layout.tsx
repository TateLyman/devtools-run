import type { Metadata } from "next";
export const metadata: Metadata = { title: "esbuild Cheatsheet - esbuild bundler reference Free", description: "esbuild bundler reference. Free esbuild Cheatsheet.", keywords: ["esbuild config", "esbuild-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
