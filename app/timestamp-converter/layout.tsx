import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Unix Timestamp Converter - Epoch to Date Online Free",
  description: "Convert Unix timestamps to human-readable dates and vice versa. Live clock, relative time, quick presets. Supports seconds and milliseconds. Free online tool.",
  keywords: ["unix timestamp converter", "epoch converter", "timestamp to date", "date to timestamp", "epoch time", "unix time converter"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
