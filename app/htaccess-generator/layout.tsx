import type { Metadata } from "next";
export const metadata: Metadata = {
  title: ".htaccess Generator - Create Apache Rewrite Rules Online",
  description: "Generate .htaccess rules for Apache. HTTPS redirect, gzip compression, browser caching, security headers, IP blocking. Free .htaccess generator.",
  keywords: [".htaccess generator", "htaccess maker", "Apache rewrite rules", "htaccess redirect", "htaccess security", "htaccess gzip"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
