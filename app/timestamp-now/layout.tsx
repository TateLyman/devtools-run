import type { Metadata } from "next";
export const metadata: Metadata = { title: "Current Timestamp - Live Unix Epoch Time Free", description: "See the current Unix timestamp updating in real-time. Copy with one click. Seconds and milliseconds. Free live timestamp.", keywords: ["current timestamp", "unix time now", "epoch now", "live timestamp", "current unix time"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
