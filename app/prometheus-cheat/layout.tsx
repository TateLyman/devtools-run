import type { Metadata } from "next";
export const metadata: Metadata = { title: "Prometheus Cheatsheet - Prometheus query reference Free", description: "Prometheus query reference. Free Prometheus Cheatsheet.", keywords: ["promql reference", "prometheus-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
