import type { Metadata } from "next";
export const metadata: Metadata = { title: "Jenkins Cheatsheet - Jenkins pipeline reference Free", description: "Jenkins pipeline reference. Free Jenkins Cheatsheet.", keywords: ["jenkinsfile", "jenkins-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
