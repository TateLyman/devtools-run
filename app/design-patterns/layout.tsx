import type { Metadata } from "next";
export const metadata: Metadata = { title: "Design Patterns - Common design patterns reference Free", description: "Common design patterns reference. Free developer reference.", keywords: ["software patterns", "design-patterns", "guide", "best practices"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
