import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "UUID Generator - Generate UUID v4 Online Free",
  description: "Generate random UUID v4 identifiers instantly. Bulk generate up to 100. Toggle hyphens and case. Cryptographically secure. Free online UUID/GUID generator.",
  keywords: ["UUID generator", "GUID generator", "random UUID", "UUID v4", "generate UUID online", "unique identifier generator"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
