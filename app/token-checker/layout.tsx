import type { Metadata } from "next";
export const metadata: Metadata = { title: "Solana Token Checker - Is This Token Safe? Free Scanner", description: "Check if a Solana token is safe. Scan for honeypots, rug pull risks, liquidity locks. Free token safety checker.", keywords: ["solana token checker", "token safety", "rug pull checker", "honeypot detector", "solana token scan", "is token safe"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
