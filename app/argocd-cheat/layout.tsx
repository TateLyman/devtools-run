import type { Metadata } from "next";
export const metadata: Metadata = { title: "ArgoCD Cheatsheet - ArgoCD GitOps reference Free", description: "ArgoCD GitOps reference. Free ArgoCD Cheatsheet.", keywords: ["argocd reference", "argocd-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
