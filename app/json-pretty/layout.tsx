import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "JSON Prettifier & Minifier - Format or Compress JSON Free",
  description: "Prettify or minify JSON instantly. Format with indentation or compress to one line. Validate JSON syntax. Free JSON formatter and minifier.",
  keywords: ["json prettifier", "json minifier", "format json", "json formatter online", "compress json", "beautify json"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
