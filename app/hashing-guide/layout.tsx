import type { Metadata } from "next";
export const metadata: Metadata = { title: "Hashing Guide - Cryptographic hashing reference Free", description: "Cryptographic hashing reference. Free Hashing Guide.", keywords: ["sha256 md5 bcrypt", "hashing-guide"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
