import type { Metadata } from "next";
export const metadata: Metadata = { title: "Streamlit Cheatsheet - Streamlit data app reference Free", description: "Streamlit data app reference. Free Streamlit Cheatsheet.", keywords: ["streamlit reference", "streamlit-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
