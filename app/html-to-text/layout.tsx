import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "HTML to Text - Strip HTML Tags Free Online",
  description: "Strip HTML tags and get plain text. Remove all markup, keep content. Free HTML to text converter and tag stripper.",
  keywords: ["html to text", "strip html tags", "remove html", "html stripper", "html to plain text", "remove tags"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
