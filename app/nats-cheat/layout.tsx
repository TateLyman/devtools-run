import type { Metadata } from "next";
export const metadata: Metadata = { title: "NATS Cheatsheet - NATS messaging reference Free", description: "NATS messaging reference. Free NATS Cheatsheet.", keywords: ["nats messaging", "nats-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
