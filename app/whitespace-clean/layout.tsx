import type { Metadata } from "next";
export const metadata: Metadata = { title: "Whitespace Remover - Clean Text Whitespace Free", description: "Remove extra whitespace, blank lines, trailing spaces. Clean messy text. Free whitespace remover.", keywords: ["whitespace remover", "remove spaces", "clean whitespace", "trim text", "remove blank lines"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
