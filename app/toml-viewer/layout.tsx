import type { Metadata } from "next";
export const metadata: Metadata = { title: "TOML Viewer - Parse and view TOML files Free Online", description: "Parse and view TOML files. Paste content, get formatted output. Free TOML Viewer.", keywords: ["view toml", "toml-viewer", "formatter", "online tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
