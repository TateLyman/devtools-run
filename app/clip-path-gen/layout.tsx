import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Clip Path Generator - Create Custom Shapes Free",
  description: "Create CSS clip-path shapes visually. Circle, ellipse, polygon, inset. Live preview. Copy CSS code. Free clip path generator.",
  keywords: ["clip path generator", "CSS clip-path", "CSS shapes", "polygon clip path", "CSS mask", "shape generator"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
