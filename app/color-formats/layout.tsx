import type { Metadata } from "next";
export const metadata: Metadata = { title: "Color Format Guide - Color format comparison guide Free", description: "Color format comparison guide. Free Color Format Guide.", keywords: ["hex rgb hsl", "color-formats"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
