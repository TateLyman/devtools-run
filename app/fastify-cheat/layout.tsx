import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fastify Cheatsheet - Fastify Node.js framework reference Free", description: "Fastify Node.js framework reference. Free Fastify Cheatsheet.", keywords: ["fastify routes", "fastify-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
