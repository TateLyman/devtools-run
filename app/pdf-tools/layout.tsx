import { Metadata } from "next";
export const metadata: Metadata = {
  title: "Free PDF Tools Online — Merge, Split, Convert PDF Files",
  description: "Free online PDF tools: merge PDFs, split pages, convert Word to PDF, and more. All tools run in your browser — your files never leave your device. No signup required.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
