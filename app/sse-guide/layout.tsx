import type { Metadata } from "next";
export const metadata: Metadata = { title: "Server-Sent Events Guide - SSE implementation guide Free", description: "SSE implementation guide. Comprehensive reference. Free Server-Sent Events Guide.", keywords: ["sse tutorial", "sse-guide", "guide", "tutorial"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
