import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "YAML Validator - Validate YAML Syntax Online Free",
  description: "Validate YAML syntax instantly. Check for tab errors, unclosed quotes, formatting issues. Free online YAML validator and linter.",
  keywords: ["YAML validator", "validate YAML", "YAML linter", "YAML checker", "YAML syntax check", "online YAML validator"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
