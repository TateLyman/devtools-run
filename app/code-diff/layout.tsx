import type { Metadata } from "next";
export const metadata: Metadata = { title: "Code Diff - Compare Code Side by Side Free", description: "Compare two code blocks side by side. See additions, deletions, changes highlighted. Free code diff tool.", keywords: ["code diff", "compare code", "code comparison", "diff tool", "code changes"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
