import type { Metadata } from "next";
export const metadata: Metadata = { title: "Z-Index Guide - Z-index stacking context guide Free", description: "Z-index stacking context guide. Visual interactive tool. Free Z-Index Guide.", keywords: ["css z-index", "z-index", "CSS tool", "visual guide"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
