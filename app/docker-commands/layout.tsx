import type { Metadata } from "next";
export const metadata: Metadata = { title: "Docker Commands - Essential Docker commands Free", description: "Essential Docker commands. Quick searchable reference. Free Docker Commands.", keywords: ["docker reference", "docker-commands", "cheatsheet", "reference guide"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
