import type { Metadata } from "next";
export const metadata: Metadata = { title: "AWS CLI Cheatsheet - Essential AWS CLI commands Free", description: "Essential AWS CLI commands. Searchable. Free AWS CLI Cheatsheet.", keywords: ["aws cli commands", "aws-cheat", "cheatsheet", "cloud reference"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
