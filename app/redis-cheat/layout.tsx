import type { Metadata } from "next";
export const metadata: Metadata = { title: "Redis Commands - Redis command reference Free", description: "Redis command reference. Searchable. Free Redis Commands.", keywords: ["redis reference", "redis-cheat", "cheatsheet", "cloud reference"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
