import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Text Shadow Generator - Create Text Effects Online",
  description: "Create beautiful CSS text shadow effects with multiple layers. Neon glow, 3D, fire, retro presets. Live preview with CSS code export. Free text shadow generator.",
  keywords: ["CSS text shadow", "text shadow generator", "neon text CSS", "text effects CSS", "CSS glow text", "text shadow tool"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
