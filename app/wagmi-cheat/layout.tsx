import type { Metadata } from "next";
export const metadata: Metadata = { title: "Wagmi Cheatsheet - Wagmi React Web3 hooks reference Free", description: "Wagmi React Web3 hooks reference. Free Wagmi Cheatsheet.", keywords: ["wagmi hooks", "wagmi-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
