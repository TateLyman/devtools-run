import type { Metadata } from "next";
export const metadata: Metadata = { title: "INI Parser - Parse INI configuration files Free Online", description: "Parse INI configuration files. Paste content, get formatted output. Free INI Parser.", keywords: ["parse ini file", "ini-parser", "formatter", "online tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
