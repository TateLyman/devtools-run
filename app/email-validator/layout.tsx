import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Email Validator - Validate Email Addresses Online Free",
  description: "Validate email addresses instantly. Check format, domain, TLD, detect disposable emails. Bulk validation supported. Free online email validator.",
  keywords: ["email validator", "validate email", "email checker", "email verification", "check email format", "bulk email validator"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
