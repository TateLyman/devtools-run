import type { Metadata } from "next";
export const metadata: Metadata = { title: "OG Image Preview - Social Share Preview Tool Free", description: "Preview how your page looks when shared on Twitter, Facebook, LinkedIn. Enter URL or set meta tags manually. Free Open Graph preview.", keywords: ["og image preview", "social share preview", "twitter card preview", "facebook share preview", "open graph preview tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
