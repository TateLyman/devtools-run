import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Number Base Converter - Convert Any Base (2-36) Free",
  description: "Convert numbers between any base from 2 to 36. Binary, octal, decimal, hex, and custom bases. Free number base converter.",
  keywords: ["base converter", "number base", "convert base", "binary to hex", "any base converter", "radix converter"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
