import type { Metadata } from "next";
export const metadata: Metadata = { title: "Country Codes - ISO country codes reference Free", description: "ISO country codes reference. Free Country Codes.", keywords: ["iso country codes", "country-codes"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
