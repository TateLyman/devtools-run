import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "package.json Generator - Create NPM Package Config Online",
  description: "Generate package.json files with scripts, dependencies, and configuration. Presets for Express, Next.js, CLI tools. Free package.json generator.",
  keywords: ["package.json generator", "npm init", "package.json maker", "npm config", "node project setup", "package.json creator"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
