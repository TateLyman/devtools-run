import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Clip-Path Generator - Shape Maker with Presets Online",
  description: "Create CSS clip-path shapes with 12 presets including triangle, hexagon, star, diamond. Custom paths supported. Free online CSS clip-path generator.",
  keywords: ["CSS clip-path", "clip-path generator", "CSS shapes", "polygon clip-path", "CSS shape maker", "clip-path presets"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
