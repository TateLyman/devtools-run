import type { Metadata } from "next";
export const metadata: Metadata = { title: "Docker Compose Cheatsheet - Docker Compose reference Free", description: "Docker Compose reference. Free Docker Compose Cheatsheet.", keywords: ["docker compose", "docker-compose-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
