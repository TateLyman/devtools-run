import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "JWT Decoder - Decode JSON Web Tokens Online Free",
  description: "Decode JWT tokens instantly. See header, payload, and signature. Check expiration. No data sent to server. Free JWT decoder.",
  keywords: ["JWT decoder", "decode JWT", "JSON web token decoder", "JWT parser", "JWT viewer", "token decoder"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
