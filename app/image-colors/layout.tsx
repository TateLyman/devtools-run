import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Extract Colors from Image - Free Color Palette Generator",
  description: "Extract dominant colors from any image. Generate color palettes from photos. Get hex codes, RGB values. Free image color extractor.",
  keywords: ["extract colors from image", "color palette from image", "image color picker", "dominant colors", "color extraction tool"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
