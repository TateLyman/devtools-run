import type { Metadata } from "next";
export const metadata: Metadata = { title: "Podman Cheatsheet - Podman container reference Free", description: "Podman container reference. Free Podman Cheatsheet.", keywords: ["podman commands", "podman-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
