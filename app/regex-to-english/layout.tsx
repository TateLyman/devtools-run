import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Regex to English - Explain Regular Expressions in Plain English",
  description: "Explain any regular expression in plain English. Understand what complex regex patterns do. Free regex explainer and translator.",
  keywords: ["regex to English", "explain regex", "regex explainer", "understand regex", "regex translator", "regex documentation"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
