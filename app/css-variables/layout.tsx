import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Variables Generator - Custom Properties Builder Free",
  description: "Generate CSS custom properties (variables) for your design system. Colors, spacing, typography, shadows. Copy :root block. Free CSS variables generator.",
  keywords: ["CSS variables", "CSS custom properties", "CSS var", "design system", "CSS tokens", "CSS variables generator"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
