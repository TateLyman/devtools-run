import type { Metadata } from "next";
export const metadata: Metadata = { title: "Binary to Text Converter - Convert Binary to ASCII Free", description: "Convert binary code to text and text to binary. ASCII conversion. Free binary to text converter.", keywords: ["binary to text", "text to binary", "binary converter", "ASCII binary", "binary decoder"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
