import type { Metadata } from "next";
export const metadata: Metadata = { title: "Text to Columns - Split text into columns Free Online", description: "Split text into columns. Process text instantly. Free Text to Columns tool.", keywords: ["split text columns", "text-columns", "text tool", "online tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
