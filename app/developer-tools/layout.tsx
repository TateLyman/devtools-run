import { Metadata } from "next";
export const metadata: Metadata = {
  title: "Free Developer Tools Online — JSON, Regex, Base64, JWT, SQL & More",
  description: "Free online developer tools: JSON formatter, regex tester, Base64 encoder, JWT decoder, SQL formatter, hash generator, and 100+ more. All browser-based, no signup.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
