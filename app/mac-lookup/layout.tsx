import type { Metadata } from "next";
export const metadata: Metadata = { title: "MAC Address Lookup - Look up MAC address vendor Free", description: "Look up MAC address vendor. Free MAC Address Lookup for developers.", keywords: ["mac address vendor", "mac-lookup", "developer tool", "free tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
