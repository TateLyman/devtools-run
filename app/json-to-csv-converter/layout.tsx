import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "JSON to CSV Converter - Convert JSON Array to CSV Free Online",
  description: "Convert JSON arrays to CSV format instantly. Download as .csv file. Handles nested objects. Free JSON to CSV converter tool online.",
  keywords: ["json to csv", "json to csv converter", "convert json to csv", "json csv online", "json array to csv"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
