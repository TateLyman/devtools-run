import type { Metadata } from "next";
export const metadata: Metadata = { title: "Apache Cheatsheet - Apache server reference Free", description: "Apache server reference. Free Apache Cheatsheet.", keywords: ["apache config", "apache-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
