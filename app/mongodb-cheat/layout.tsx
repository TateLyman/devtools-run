import type { Metadata } from "next";
export const metadata: Metadata = { title: "MongoDB Cheatsheet - MongoDB query reference Free", description: "MongoDB query reference. Searchable. Free MongoDB Cheatsheet.", keywords: ["mongodb queries", "mongodb-cheat", "cheatsheet", "cloud reference"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
