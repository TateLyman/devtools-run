import type { Metadata } from "next";
export const metadata: Metadata = { title: "Hex Viewer - View Text as Hexadecimal Free", description: "View any text as hexadecimal. See hex dump with ASCII. Free hex viewer tool.", keywords: ["hex viewer", "hex dump", "text to hex", "hexadecimal viewer", "hex editor online"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
