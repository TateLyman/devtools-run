import type { Metadata } from "next";
export const metadata: Metadata = { title: "IP to Binary - Convert IP address to binary Free", description: "Convert IP address to binary. Free IP to Binary for developers.", keywords: ["ip address binary", "ip-to-binary", "developer tool", "free tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
