import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "URL Slug Generator - Convert Text to URL-Friendly Slugs Free",
  description: "Convert any text to a clean URL slug. Remove special characters, replace spaces with hyphens, lowercase. Free URL slug generator.",
  keywords: ["slug generator", "url slug", "text to slug", "permalink generator", "url friendly", "slug converter"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
