import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Countdown Timer to Any Date - Free Online Countdown Clock",
  description: "Create a countdown timer to any date. Days, hours, minutes, seconds. Share countdown link. Free countdown timer to event, birthday, holiday.",
  keywords: ["countdown timer", "countdown to date", "days until", "countdown clock", "event countdown", "date countdown"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
