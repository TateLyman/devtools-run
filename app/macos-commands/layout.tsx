import type { Metadata } from "next";
export const metadata: Metadata = { title: "macOS Commands - macOS terminal commands reference Free", description: "macOS terminal commands reference. Free macOS Commands.", keywords: ["macos terminal", "macos-commands", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
