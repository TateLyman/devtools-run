import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Grid Generator - Visual Grid Layout Builder Free",
  description: "Create CSS Grid layouts visually. Set columns, rows, gap. See live preview. Copy CSS code. Free CSS Grid generator and playground.",
  keywords: ["CSS grid generator", "grid layout builder", "CSS grid playground", "grid template", "CSS grid visual", "grid layout tool"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
