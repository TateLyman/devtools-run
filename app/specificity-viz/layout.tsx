import type { Metadata } from "next";
export const metadata: Metadata = { title: "CSS Specificity Visualizer - Compare Selectors Side by Side Free", description: "Compare CSS selector specificity visually. See which selector wins. A-B-C scoring with colors. Free CSS specificity visualizer.", keywords: ["CSS specificity", "specificity visualizer", "css selector priority", "specificity calculator visual", "which css wins"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
