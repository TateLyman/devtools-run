import type { Metadata } from "next";
export const metadata: Metadata = { title: "Docker Compose Generator - Generate docker-compose.yml files Free", description: "Generate docker-compose.yml files. Generate configs and templates easily. Free Docker Compose Generator.", keywords: ["docker compose", "docker-compose-gen", "generator", "devops tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
