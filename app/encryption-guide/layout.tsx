import type { Metadata } from "next";
export const metadata: Metadata = { title: "Encryption Guide - Encryption algorithms reference Free", description: "Encryption algorithms reference. Free Encryption Guide.", keywords: ["aes rsa", "encryption-guide"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
