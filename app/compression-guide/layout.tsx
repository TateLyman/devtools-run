import type { Metadata } from "next";
export const metadata: Metadata = { title: "Compression Guide - File compression comparison Free", description: "File compression comparison. Free Compression Guide.", keywords: ["gzip brotli", "compression-guide"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
