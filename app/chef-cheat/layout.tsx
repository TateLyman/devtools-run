import type { Metadata } from "next";
export const metadata: Metadata = { title: "Chef Cheatsheet - Chef automation reference Free", description: "Chef automation reference. Free Chef Cheatsheet.", keywords: ["chef reference", "chef-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
