import type { Metadata } from "next";
export const metadata: Metadata = { title: "Regex Visualizer - See Regex Matches Highlighted Free", description: "Visualize regex matches in real-time. See which parts of your text match. Highlight groups. Free regex visualization tool.", keywords: ["regex visualizer", "regex highlight", "regex match visualizer", "regex tester visual", "regular expression visualizer"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
