import type { Metadata } from "next";
export const metadata: Metadata = { title: "MLflow Cheatsheet - MLflow ML lifecycle reference Free", description: "MLflow ML lifecycle reference. Free MLflow Cheatsheet.", keywords: ["mlflow reference", "mlflow-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
