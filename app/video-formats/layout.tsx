import type { Metadata } from "next";
export const metadata: Metadata = { title: "Video Format Guide - Video format comparison guide Free", description: "Video format comparison guide. Free Video Format Guide.", keywords: ["mp4 webm", "video-formats"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
