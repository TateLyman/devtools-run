import type { Metadata } from "next";
export const metadata: Metadata = { title: "dbt Cheatsheet - dbt data transformation reference Free", description: "dbt data transformation reference. Free dbt Cheatsheet.", keywords: ["dbt reference", "dbt-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
