import type { Metadata } from "next";
export const metadata: Metadata = { title: "Kubernetes Cheatsheet - Essential kubectl commands Free", description: "Essential kubectl commands. Quick searchable reference. Free Kubernetes Cheatsheet.", keywords: ["kubectl commands", "kubernetes-cheat", "cheatsheet", "reference guide"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
