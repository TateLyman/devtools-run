import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Cron Expression Explainer - What Does This Cron Do? Free",
  description: "Paste a cron expression and get a human-readable explanation. See next run times. Free cron expression explainer and parser.",
  keywords: ["cron expression explainer", "cron expression parser", "what does cron mean", "crontab explainer", "cron to english"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
