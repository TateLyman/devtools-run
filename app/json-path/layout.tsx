import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "JSON Path Finder - Click to Get JSON Path Free",
  description: "Paste JSON, click any value to get its path. JSONPath notation. Copy paths instantly. Free JSON path finder and explorer tool.",
  keywords: ["json path", "json path finder", "jsonpath", "json explorer", "json tree", "find json path"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
