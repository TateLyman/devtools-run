import type { Metadata } from "next";
export const metadata: Metadata = { title: "Audio Format Guide - Audio format comparison guide Free", description: "Audio format comparison guide. Free Audio Format Guide.", keywords: ["mp3 wav flac", "audio-formats"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
