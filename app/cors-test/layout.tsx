import type { Metadata } from "next";
export const metadata: Metadata = { title: "CORS Tester - Test CORS headers on any URL Free Online", description: "Test CORS headers on any URL. Free online CORS Tester for developers.", keywords: ["test cors headers", "cors-test", "api tool", "network tool", "developer tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
