import type { Metadata } from "next";
export const metadata: Metadata = { title: "TLS/SSL Guide - TLS/SSL protocol reference Free", description: "TLS/SSL protocol reference. Free TLS/SSL Guide.", keywords: ["tls ssl", "tls-guide", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
