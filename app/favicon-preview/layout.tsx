import type { Metadata } from "next";
export const metadata: Metadata = { title: "Favicon Preview - See How Your Favicon Looks in Browser Tabs Free", description: "Preview how your favicon looks in browser tabs, bookmarks, and search results. Upload or paste URL. Free favicon preview tool.", keywords: ["favicon preview", "favicon tester", "browser tab favicon", "favicon mockup", "favicon check"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
