import type { Metadata } from "next";
export const metadata: Metadata = { title: "gRPC Cheatsheet - gRPC protocol reference Free", description: "gRPC protocol reference. Free gRPC Cheatsheet.", keywords: ["grpc reference", "grpc-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
