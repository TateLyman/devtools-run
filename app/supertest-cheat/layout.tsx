import type { Metadata } from "next";
export const metadata: Metadata = { title: "Supertest Cheatsheet - Supertest API testing reference Free", description: "Supertest API testing reference. Free Supertest Cheatsheet.", keywords: ["supertest api", "supertest-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
