import type { Metadata } from "next";
export const metadata: Metadata = { title: "Text Truncator - Truncate text to specific length Free Online", description: "Truncate text to specific length. Process text instantly. Free Text Truncator tool.", keywords: ["truncate text", "text-truncator", "text tool", "online tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
