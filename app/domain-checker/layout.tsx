import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Domain Name Checker - Check Availability Across 10 TLDs Free",
  description: "Check domain availability across .com, .io, .dev, .ai, .co, .app, .xyz, .tech, .org, .net instantly. Free domain name availability checker.",
  keywords: ["domain checker", "domain availability", "check domain name", "domain name search", "is domain available", "domain lookup"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
