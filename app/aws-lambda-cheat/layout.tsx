import type { Metadata } from "next";
export const metadata: Metadata = { title: "AWS Lambda Cheatsheet - AWS Lambda reference Free", description: "AWS Lambda reference. Free AWS Lambda Cheatsheet.", keywords: ["lambda functions", "aws-lambda-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
