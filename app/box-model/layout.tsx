import type { Metadata } from "next";
export const metadata: Metadata = { title: "CSS Box Model Visualizer - Interactive Margin, Padding, Border Free", description: "Interactive CSS box model visualizer. Adjust margin, padding, border, width, height. See the box model update live. Copy CSS. Free tool.", keywords: ["CSS box model", "box model visualizer", "margin padding border", "CSS box model interactive", "box model tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
