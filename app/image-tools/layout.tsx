import { Metadata } from "next";
export const metadata: Metadata = {
  title: "Free Image Tools Online — Compress, Resize, Convert, Remove Background",
  description: "Free online image tools: compress images, resize photos, convert formats, remove backgrounds, and more. All processing happens in your browser — files never leave your device.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
