import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Meta Tag Generator - SEO, Open Graph & Twitter Card Tags",
  description: "Generate HTML meta tags for SEO, Open Graph, and Twitter Cards. Google search preview. Free online meta tag generator with copy button.",
  keywords: ["meta tag generator", "SEO meta tags", "Open Graph generator", "Twitter Card generator", "HTML meta tags", "og tags generator"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
