import type { Metadata } from "next";
export const metadata: Metadata = { title: "Font Format Guide - Web font format guide Free", description: "Web font format guide. Free Font Format Guide.", keywords: ["woff2 ttf otf", "font-formats"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
