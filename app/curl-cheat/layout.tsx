import type { Metadata } from "next";
export const metadata: Metadata = { title: "cURL Cheatsheet - cURL request reference Free", description: "cURL request reference. Free cURL Cheatsheet.", keywords: ["curl commands", "curl-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
