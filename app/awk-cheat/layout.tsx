import type { Metadata } from "next";
export const metadata: Metadata = { title: "AWK Cheatsheet - AWK text processing reference Free", description: "AWK text processing reference. Free AWK Cheatsheet.", keywords: ["awk commands", "awk-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
