import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Gradient Generator - Create Linear, Radial & Conic Gradients",
  description: "Create beautiful CSS gradients with multiple color stops. Linear, radial, and conic. 6 presets, custom angles. Copy the CSS code. Free gradient generator.",
  keywords: ["CSS gradient generator", "gradient maker", "linear gradient", "radial gradient", "conic gradient", "CSS background gradient"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
