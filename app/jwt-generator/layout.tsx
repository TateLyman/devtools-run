import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "JWT Generator - Create JSON Web Tokens Free Online",
  description: "Generate JWT tokens with custom claims, expiration, and signing. HS256 algorithm. Free JWT token generator for testing and development.",
  keywords: ["JWT generator", "create JWT", "JSON web token generator", "JWT maker", "JWT token creator", "test JWT"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
