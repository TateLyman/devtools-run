import type { Metadata } from "next";
export const metadata: Metadata = { title: "Font Stack Generator - Generate safe font stacks Free", description: "Generate safe font stacks. Visual interactive tool. Free Font Stack Generator.", keywords: ["css font stack", "font-stack", "CSS tool", "visual guide"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
