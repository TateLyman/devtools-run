import type { Metadata } from "next";
export const metadata: Metadata = { title: "CIDR Calculator - Calculate CIDR ranges and subnets Free", description: "Calculate CIDR ranges and subnets. Free CIDR Calculator for developers.", keywords: ["cidr range", "cidr-calc", "developer tool", "free tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
