import type { Metadata } from "next";
export const metadata: Metadata = { title: "DNS Lookup Tool - Look up DNS records for any domain Free Online", description: "Look up DNS records for any domain. Free online DNS Lookup Tool for developers.", keywords: ["dns lookup", "dns-check", "api tool", "network tool", "developer tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
