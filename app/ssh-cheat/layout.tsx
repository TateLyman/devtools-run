import type { Metadata } from "next";
export const metadata: Metadata = { title: "SSH Cheatsheet - SSH command reference Free", description: "SSH command reference. Free SSH Cheatsheet.", keywords: ["ssh commands", "ssh-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
