import type { Metadata } from "next";
export const metadata: Metadata = { title: "PostgreSQL Cheatsheet - PostgreSQL reference Free", description: "PostgreSQL reference. Free PostgreSQL Cheatsheet.", keywords: ["postgresql commands", "postgres-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
