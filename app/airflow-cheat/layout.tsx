import type { Metadata } from "next";
export const metadata: Metadata = { title: "Airflow Cheatsheet - Apache Airflow DAG reference Free", description: "Apache Airflow DAG reference. Free Airflow Cheatsheet.", keywords: ["apache airflow", "airflow-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
