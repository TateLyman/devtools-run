import type { Metadata } from "next";
export const metadata: Metadata = { title: "JWT Inspector - Decode & Verify JWT Tokens Free", description: "Inspect JWT tokens. See header, payload, signature. Check expiration. Verify HS256 signatures. Free JWT inspector.", keywords: ["JWT inspector", "JWT verify", "decode JWT", "JWT debugger", "token inspector"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
