import type { Metadata } from "next";
export const metadata: Metadata = { title: "Currency Codes - ISO currency codes reference Free", description: "ISO currency codes reference. Free Currency Codes.", keywords: ["iso currency", "currency-codes"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
