import type { Metadata } from "next";
export const metadata: Metadata = { title: "ASCII Table - Complete ASCII character reference table Free", description: "Complete ASCII character reference table. Searchable reference. Free ASCII Table.", keywords: ["ascii codes", "ascii-table", "reference", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
