import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "String Escape/Unescape Tool - JSON, HTML, URL, Regex Online Free",
  description: "Escape and unescape strings for JSON, HTML, URL, SQL, CSV, and regex. Free online string escaping tool. Handles special characters, quotes, and Unicode. No data sent to servers.",
  keywords: ["string escape", "unescape string", "JSON escape", "HTML escape", "URL encode", "regex escape", "SQL escape", "string encoder", "escape special characters"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
