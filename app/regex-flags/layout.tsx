import type { Metadata } from "next";
export const metadata: Metadata = { title: "Regex Flags Guide - Guide to regex flags (g i m s u) Free", description: "Guide to regex flags (g i m s u). Searchable reference. Free Regex Flags Guide.", keywords: ["regex flags", "regex-flags", "reference", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
