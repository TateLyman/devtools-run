import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "UTM Builder - Campaign URL Generator Free",
  description: "Build UTM tracking URLs for Google Analytics. Set source, medium, campaign, content, term. Copy tracking link. Free UTM builder.",
  keywords: ["UTM builder", "UTM generator", "campaign URL", "Google Analytics UTM", "tracking URL", "UTM parameters"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
