import type { Metadata } from "next";
export const metadata: Metadata = { title: "GSAP Cheatsheet - GSAP animation reference Free", description: "GSAP animation reference. Free GSAP Cheatsheet.", keywords: ["gsap animation", "gsap-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
