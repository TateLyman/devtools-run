import type { Metadata } from "next";
export const metadata: Metadata = { title: "Font Pairing Tool - Find Google Font Combinations Free", description: "Find beautiful font pairings for your design. Preview heading + body combinations. Google Fonts. Copy CSS. Free font pairing tool.", keywords: ["font pairing", "google fonts pairing", "font combination", "heading body font", "typography pairing"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
