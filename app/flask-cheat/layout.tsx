import type { Metadata } from "next";
export const metadata: Metadata = { title: "Flask Cheatsheet - Flask Python framework reference Free", description: "Flask Python framework reference. Free Flask Cheatsheet.", keywords: ["flask reference", "flask-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
