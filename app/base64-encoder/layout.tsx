import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Base64 Encoder / Decoder - Encode & Decode Base64 Online",
  description: "Encode and decode Base64 strings online for free. Supports UTF-8 text. Instant conversion as you type. Free Base64 encoder and decoder tool.",
  keywords: ["base64 encoder", "base64 decoder", "base64 converter", "encode base64 online", "decode base64", "base64 tool"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
