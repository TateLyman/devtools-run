import type { Metadata } from "next";
export const metadata: Metadata = { title: "Authentication Guide - Authentication methods compared Free", description: "Authentication methods compared. Free developer reference.", keywords: ["auth methods", "auth-guide", "guide", "best practices"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
