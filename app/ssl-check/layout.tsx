import type { Metadata } from "next";
export const metadata: Metadata = { title: "SSL Certificate Checker - Check SSL certificate expiry and details Free Online", description: "Check SSL certificate expiry and details. Free online SSL Certificate Checker for developers.", keywords: ["check ssl certificate", "ssl-check", "api tool", "network tool", "developer tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
