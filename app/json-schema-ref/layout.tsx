import type { Metadata } from "next";
export const metadata: Metadata = { title: "JSON Schema Reference - JSON Schema validation reference Free", description: "JSON Schema validation reference. Comprehensive reference. Free JSON Schema Reference.", keywords: ["json schema", "json-schema-ref", "guide", "tutorial"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
