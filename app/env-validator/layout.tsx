import type { Metadata } from "next";
export const metadata: Metadata = { title: "ENV Validator - Validate .env files for missing keys Free", description: "Validate .env files for missing keys. Generate configs and templates easily. Free ENV Validator.", keywords: ["validate env file", "env-validator", "generator", "devops tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
