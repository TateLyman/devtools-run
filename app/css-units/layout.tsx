import type { Metadata } from "next";
export const metadata: Metadata = { title: "CSS Unit Converter - PX, REM, EM, VW, VH, PT Free", description: "Convert between CSS units: px, rem, em, vw, vh, pt, cm, mm, in. Free CSS unit converter.", keywords: ["CSS units", "px to rem", "rem to px", "CSS unit converter", "vw to px", "em to px"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
