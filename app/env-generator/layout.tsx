import type { Metadata } from "next";
export const metadata: Metadata = {
  title: ".env File Generator - Create Environment Variable Files Online",
  description: "Generate .env files from templates. Next.js, Express, Django, React, Rails presets. Download .env and .env.example. Free .env generator.",
  keywords: [".env generator", "environment variables", "env file maker", "dotenv generator", ".env template", "env file creator"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
