import type { Metadata } from "next";
export const metadata: Metadata = { title: "Heroku CLI Reference - Heroku CLI quick reference Free", description: "Heroku CLI quick reference. Searchable. Free Heroku CLI Reference.", keywords: ["heroku commands", "heroku-cheat", "cheatsheet", "cloud reference"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
