import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Webhook Tester - Send HTTP Requests to Any URL Online",
  description: "Send HTTP requests to any URL. Test webhooks, APIs, and endpoints. GET, POST, PUT, PATCH, DELETE. Custom headers and body. Free webhook tester.",
  keywords: ["webhook tester", "HTTP request tester", "API tester", "webhook debugger", "send HTTP request", "test webhook online"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
