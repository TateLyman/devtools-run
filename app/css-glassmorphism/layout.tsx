import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Glassmorphism Generator - Glass Effect UI Builder",
  description: "Create beautiful glass-effect UI components with customizable blur, opacity, saturation, and border. Live preview with CSS code export. Free glassmorphism generator.",
  keywords: ["glassmorphism", "glass effect CSS", "glassmorphism generator", "frosted glass CSS", "backdrop-filter", "CSS glass card"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
