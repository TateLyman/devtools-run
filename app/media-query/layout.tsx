import type { Metadata } from "next";
export const metadata: Metadata = { title: "Media Query Generator - Generate CSS media queries Free", description: "Generate CSS media queries. Visual interactive tool. Free Media Query Generator.", keywords: ["responsive breakpoints", "media-query", "CSS tool", "visual guide"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
