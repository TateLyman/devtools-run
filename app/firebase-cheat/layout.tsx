import type { Metadata } from "next";
export const metadata: Metadata = { title: "Firebase CLI Reference - Firebase CLI reference Free", description: "Firebase CLI reference. Searchable. Free Firebase CLI Reference.", keywords: ["firebase commands", "firebase-cheat", "cheatsheet", "cloud reference"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
