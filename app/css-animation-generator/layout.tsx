import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Animation Generator - Browse & Customize CSS Animations",
  description: "12 CSS animation presets with live preview. Customize duration, timing, delay, and iterations. Copy generated CSS keyframes. Free online animation tool.",
  keywords: ["CSS animation generator", "CSS keyframes", "CSS animation presets", "animation builder", "CSS motion", "web animation tool"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
