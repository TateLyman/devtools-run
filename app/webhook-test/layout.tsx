import type { Metadata } from "next";
export const metadata: Metadata = { title: "Webhook Tester - Send test webhooks to any URL Free Online", description: "Send test webhooks to any URL. Free online Webhook Tester for developers.", keywords: ["test webhooks", "webhook-test", "api tool", "network tool", "developer tool"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
