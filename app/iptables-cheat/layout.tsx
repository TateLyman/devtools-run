import type { Metadata } from "next";
export const metadata: Metadata = { title: "IPTables Cheatsheet - Linux firewall reference Free", description: "Linux firewall reference. Free IPTables Cheatsheet.", keywords: ["iptables rules", "iptables-cheat", "cheatsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
