import type { Metadata } from "next";
export const metadata: Metadata = { title: "Jotai Cheatsheet - Jotai atomic state reference Free", description: "Jotai atomic state reference. Free Jotai Cheatsheet.", keywords: ["jotai state", "jotai-cheat"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
