import type { Metadata } from "next";
export const metadata: Metadata = { title: "CSS Cursor Playground - All CSS Cursors Interactive Demo Free", description: "See all CSS cursor types in one place. Hover to preview each cursor. Copy CSS. Free CSS cursor playground.", keywords: ["CSS cursors", "cursor playground", "cursor types CSS", "CSS cursor demo", "all CSS cursors"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
