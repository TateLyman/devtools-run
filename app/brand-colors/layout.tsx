import type { Metadata } from "next";
export const metadata: Metadata = { title: "Brand Color Extractor - Extract Colors from Any Website Free", description: "Extract brand colors from any website. Get the dominant colors, hex codes, and CSS variables. Free brand color extractor.", keywords: ["brand colors", "extract colors website", "website color palette", "brand color extractor", "color scheme extractor"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
